datasource db {
  provider = "sqlite"
  url      = "file:Data.db"
}

generator client {
  provider = "cargo prisma"
  output   = "../src-tauri/src/client.rs"
}

model File {
  id   Int    @id @default(autoincrement())
  name String
  ext  String
  size Int
}

model Folder {
  id       Int     @id @default(autoincrement())
  name     String
  favorite Boolean
}
